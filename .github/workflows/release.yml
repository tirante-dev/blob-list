name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  release:
    name: release
    runs-on: homelab-2cpu-8ram
    steps:
      - name: Install libatomic for Node
        run: sudo apt-get -qq update && sudo apt-get -qq install -y --no-install-recommends libatomic1
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 26
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate artifacts
        run: npm run generate

      - name: Validate
        run: npm run validate

      - name: Check artifacts
        run: npm run generate:check

      - name: Build checksums
        run: |
          find artifacts -type f -name '*.json' -print0 \
            | sort -z \
            | xargs -0 shasum -a 256 > SHA256SUMS

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v3
        with:
          files: |
            artifacts/*.json
            artifacts/by-chain/*.json
            SHA256SUMS
