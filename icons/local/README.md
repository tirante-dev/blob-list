# Local Icons

Use this directory only for entities that are not represented by Chainlist.

Local icons must be SVG files, must not contain remote references or embedded raster data, and must include `license` and `source_url` metadata in the entity YAML.
